@using PurpleBuzz_Backend.Models
@model ContactIntro
<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-md-6 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <p class="card-description">
                            Delete Intro
                        </p>

                        <span>Title:</span>
                        <span>@Model.Title</span>

                        <br />
                        <br />
                        <span>Primary Text:</span>
                        <span>@Model.PrimaryText</span>
                        <br />
                        <br />
                        <span>Description:</span>
                        <span>@Model.Description</span>
                        <br />
                        <br />
                        <span>
                            <img src="~/assets/img/@Model.PhotoName" />
                        </span>
                        <form method="post" asp-action="deleteintro" asp-controller="contactintro" asp-area="admin" asp-route-id="@Model.Id">
                            <button onclick="confirm('Əminsiz mi?')" class="btn btn-danger">Silin</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>